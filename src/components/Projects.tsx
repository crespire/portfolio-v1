import { FC } from 'react';
import { projects } from '../projects';

const Projects: FC = () => {
  return (
    <section id="projects" className="grid grid-cols-3 grid-rows-2 h-[93.5vh]">
        PROJECTS
    </section>
  );
};

export default Projects;